<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true">
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:gravity="center"
        android:clickable="false"
        android:visibility="visible"
        android:orientation="vertical"
        android:background="@color/White"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">
        <TextView
            android:id="@+id/icon_connection"
            android:text="@string/icon_cloud_error"
            android:textSize="@dimen/text_size_80"
            android:textColor="@color/LightGrey"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <TextView
            android:id="@+id/message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="@dimen/padding_20_dp"
            android:textSize="@dimen/text_size_16"
            android:fontFamily="sans-serif"
            android:lineSpacingExtra="@dimen/margin_3_dp"
            android:textColor="@color/Gray"
            android:gravity="center"
            android:text="Something went wrong. Plase try again later.\nOR\nswipe down to refresh."/>
    </LinearLayout>
</ScrollView>